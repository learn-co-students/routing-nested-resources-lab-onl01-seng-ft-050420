<% if @song.artist %>
    <h1><%= link_to @song.title, artist_song_path(@song.artist, @song) if @song.artist %></h1>
    <h3> by <%= display_artist(@song) %></h3>
<% else %>
     <h1><%= @song.title%></h1>
    <h3><%= display_artist(@song) %></h3>
<% end %>

<%= link_to "All Songs", songs_path %>
